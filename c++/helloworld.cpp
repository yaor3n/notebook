#include <iostream>

int main() {

    // std::cout = standard character output
    std::cout << "hello world" << std::endl;
    std::cout << "c++ is kinda fun" << "\n";
    std::cout << "\\n is better for performance compared to endl" << "\n";
    std::cout << "but the benefits of using endl is that it flushes the output buffer";

    return 0;
    // if it returns 0, there is no problems. if return there is an error
}