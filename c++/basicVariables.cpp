#include <iostream>
#include <string> // include when using std::string

int main() {

    int x; // declaration
    x = 5; // assignment

    int y = 10; // integer, whole number 

    double a = 12.99; // decimals

    char grade = 'A'; // must be in single quotes '' because its a char literal and not a string ""

    bool human = true;
    bool cat = false;

    // strings are abit diff
    std::string name = "yaoren";

    std::cout << "hello " << name;
}